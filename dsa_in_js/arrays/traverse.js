const arr = [3, 6, 4, 12];
for (let i = 0; i < arr.length; i++) {
  //   document.write(`Array ${i} is ${arr[i]}`); // document is not defined for node env
  console.log(`Array ${i} is ${arr[i]}`);   
}
